модули:
1) модуль обработки символа tab(обобщенный модуль, работает не только с символом tab)
2) модуль поиска(сервис, который ищет исполняемые файлы по пути)

Требования:
1) ввод символа tab
2) вызывается модуль автоподстановки
	1) проверяем содержимое строки
		a) строка пустая или в ней содержатся только символы табуляции
			.) вводится символ табуляции(при вводе только символов табуляции в хистори не идут)
			.) сценарий завершается, управление передается вызвавшему модулю(вводим символ tab выходим из ввода символа tab)
		b) в стркое есть содержимое
			.) запускается сервис поиска
				a) сервис поиска вернул пустое значение
					.) ничего не происходит, сценарий завершается, управление передается вызвавшему модулю
				b) сервис поиска вернул содержимое
					.) возвращаем содержимое, сценарий завершается, управление передается вызывавшему модулю
	2) вызывается модуль обработки вернувшегося содержимого
		a) содержимое пустое
			.) ничего не происходит, модуль автоподстановки завершается
		b) содержимое состоит из одного элемента
			.) элемент вставляется в строку
			.) модуль автоподстановки завершается
		c) содержимое состоит из n-ого количества элементов(где n > 1)
			.) вызывается модуль ft_select
			.) выбирается элемент
			.) запускается обработчик клавиш в ft_select режиме
			.) (дополнить!!!)